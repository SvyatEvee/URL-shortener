<div align="center">
  <h1>🔗 URL Shortener</h1>
  <p>
    <strong>Сервис для создания коротких ссылок с личным кабинетом</strong>
  </p>
</div>

---

## 📋 О проекте

Сервис для создания коротких ссылок с возможностью управления ими в личном кабинете. Проект представляет собой полноценное веб-приложение (бэкенд + фронтенд), позволяющее пользователям регистрироваться, создавать, редактировать и удалять свои ссылки.

## ✨ Функциональность

-   **🔐 Регистрация и авторизация пользователей.** Личный кабинет, защищенный JWT-токенами.
-   **🔨 Создание коротких ссылок.** Превратите длинный и сложный URL в лаконичную ссылку.
-   **📝 Управление ссылками.** Просмотр, редактирование и удаление созданных вами ссылок.
-   **📱 Доступность.** Доступ к вашим ссылкам с любого устройства после входа в аккаунт.

## 🛠 Стек технологий

*   **Backend:** Go (REST API, gRPC)
*   **Frontend:** JavaScript
*   **База данных:** PostgreSQL
*   **Инфраструктура:** Docker, Docker Compose, Nginx, GitHub Actions
*   **Безопасность:** JWT, SSL/TTS

## 🚀 Начало работы

### Предварительные требования

На вашем хосте должны быть обязательно установлены:
-   **Docker** 🐳
-   **Docker Compose** 🐙

### Установка и запуск

1.  **Клонируйте репозиторий:**

    ```bash
    git clone https://github.com/SvyatEvee/URL-shortener
    ```

2.  **Перейдите в директорию проекта:**

    ```bash
    cd URL-shortener
    ```

3.  **Создайте .env файл в главной директории проекта**

    Создайте .env файл, необходимый для корректной работы docker compose, на основе файла-примера .env.example

    ```bash
    cp .env.example .env
    ```

4.  **⚙️ Конфигурация для локального запуска**

    По умолчанию в проекте используется конфигурация Nginx с поддержкой SSL/TLS для работы по HTTPS. Если вы хотите запустить проект локально (на `localhost`) для разработки или тестирования, выполните следующие действия:

    -   Перейдите в директорию `./nginx/conf/`.
    -   Замените содержимое файла `nginx.conf` содержимым файла `nginx_local.conf`.
    -   Перезапустите контейнеры.

> **💡 Примечание:** Конфигурация `nginx_local.conf` предназначена только для локального использования. Она настроена на прослушивание порта `80` loopback адреса `127.0.0.1` без шифрования (HTTP).

5.  **Запустите проект с помощью Docker Compose:**

    ```bash
    docker compose -f compose_local.yml up -d
    ```

> **💡 Для ОС Windows:** При работе на ОС Windows убедитесь, что файл `./nginx/docker-entrypoint.sh` сохранён с правильными окончаниями строк в формате **LF**, а не CRLF. Неверный формат (CRLF) приводит к ошибке выполнения скрипта внутри контейнера.

6.  **🌐 Приложение будет доступно по адресу:** `http://localhost`.

> **💡 Примечание:** При локальном развертывании проброс портов на хост ограничен Nginx (80) и контейнерами PostgreSQL (порты задаются в .env). Все опубликованные порты прослушивают только loopback-адреса (127.0.0.1), исключая внешний доступ к сервисам.